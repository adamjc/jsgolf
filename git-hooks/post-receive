# Copy files across to /var/jsgolf
mkdir -p /var/jsgolf
git clone /git /var/jsgolf
cd /var/jsgolf
cp git-hooks/post-recieve /git/hooks/

apt install -y npm
npm install -g npm@latest
npm install
npx gulp
npm start

# Copy NGINX config across
# Restart NGINX
