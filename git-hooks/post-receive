# Copy files across to /var/jsgolf
mkdir -p /var/jsgolf
cp -r /git/ /var/jsgolf
npm start

# Copy NGINX config across
# Restart NGINX
# Copy post-receive hook (this file) to /git/hooks/
cp git-hooks/post-recieve /git/hooks/
